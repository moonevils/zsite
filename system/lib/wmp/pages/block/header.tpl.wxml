<template name="header">
  <view class="nav space-sm shadow-1">
    <block wx:for="{{block.navs}}" wx:key="uniq">
        <navigator wx:if="{{item.type === 'system'}}" class="item">{{item.title}}</navigator>
    </block>
  </view>
</template>